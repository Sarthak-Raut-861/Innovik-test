version: "3.8"

services:
  # Reference Mock Ingestion Service & Test Harness
  test-server:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    command: node example/mock-server.js

  # Placeholder for Phase 2: Production TrustPulse Telemetry Ingestion Gateway
  # api-gateway:
  #   build: ./backend
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - DATABASE_URL=postgres://trustpulse:secret@postgres:5432/trustpulse
  #     - REDIS_URL=redis://redis:6379/0
  #   depends_on:
  #     - postgres
  #     - redis

  # Placeholder for Phase 2: Session Cache & Telemetry Queue
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"

  # Placeholder for Phase 2: Long-term Behavioral Profile Storage
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: trustpulse
  #     POSTGRES_PASSWORD: secret_change_in_prod
  #     POSTGRES_DB: trustpulse
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# volumes:
#   pgdata:
